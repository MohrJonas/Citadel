name: citadel
description: "Shared citadel environment variables"
config:
  environment.HOME: /home/citadel
  environment.XDG_RUNTIME_DIR: /run/user/${user_uid}
  environment.DBUS_SESSION_BUS_ADDRESS: unix:path=/run/user/${user_uid}/bus
